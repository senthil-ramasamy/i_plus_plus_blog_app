<%= form_for(@todocontent) do |f| %>
  <% if @todocontent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todocontent.errors.count, "error") %> prohibited this todocontent from being saved:</h2>

      <ul>
      <% @todocontent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
